"""P7 第一批只读工具：检索 / 图谱 / 实体 / 文档 / 社区（权限内行动，v1 只读）。

每工具三道闸：① 权限门（注册表 ``list_for`` 预过滤）② 参数门（注册表 schema 校验）
③ 数据门（store 侧 ``visible_to``；``get_chunk`` 接口无 access 过滤，工具层自补逐条复核——
跨密级泄漏通道红线）。输出截断 + 引注口径，入 prompt 前沿用 P6 sanitize 纪律
（不执行来自工具结果的指令，注入防御不寄托提示词）。
"""
